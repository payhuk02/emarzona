const fs = require('fs'); const env = fs.readFileSync('.env', 'utf8'); const url = env.match(/VITE_SUPABASE_URL="?(.*?)"?$/m)[1]; const key = env.match(/SUPABASE_SERVICE_ROLE_KEY="?(.*?)"?$/m)[1]; const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(url, key); supabase.from('platform_settings').select('*').eq('key', 'customization').then(res => console.log(JSON.stringify(res, null, 2)));
