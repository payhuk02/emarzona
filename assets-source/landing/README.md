# Landing design sources (not bundled)

PNG / master files used to generate optimized WebP assets under `src/assets/landing/`.
Keep these out of `src/` so Vite never includes them in production bundles.
