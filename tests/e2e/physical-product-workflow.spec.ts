/**
 * @deprecated Remplacé par tests/e2e/physical-product-publish.spec.ts (Sprint 2 — seed Supabase, sans credentials hardcodés).
 */
import { test } from '@playwright/test';

test.describe('Physical product workflow (legacy alias)', () => {
  test.skip(true, 'Voir tests/e2e/physical-product-publish.spec.ts');
});
