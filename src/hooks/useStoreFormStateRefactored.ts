/**
 * Store form state — implémentation canonique (useReducer migration Sprint 3).
 * StoreDetails consomme ce hook via l'alias useStoreFormStateRefactored.
 */
export {
  useStoreFormState,
  useStoreFormState as useStoreFormStateRefactored,
} from './useStoreFormState';
